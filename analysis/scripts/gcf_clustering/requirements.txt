pandas
numpy
scipy
scikit-learn
tqdm
biopython
matplotlib

# Not installable via pip — install through conda/mamba:
#   conda install -c bioconda -c conda-forge hmmer
# Required by 03_backbone_identity.py (hmmalign, hmmfetch, hmmscan).